package com.pedalboard.recreator.data

enum class ChainStage { PRE_AMP, AMP, FX_LOOP }
enum class ConnectionType { MONO, STEREO }
enum class ChannelType { MONO, STEREO, LEFT, RIGHT }
enum class SignalState { MONO, STEREO, SPLIT }
